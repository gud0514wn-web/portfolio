v54 / Apps Script v16

추가:
- 자산변화 탭 상단에 목표자산 진행률 카드 추가.
- 현재자산 / 목표금액 / 현재 달성률(%) 표시.
- 아래에 가로 진행바 표시. 100%를 넘으면 숫자는 실제 달성률, 바는 100%까지 표시.
- 목표금액을 누르면 억원 단위로 바로 수정 가능.
- 목표금액은 PC/모바일 클라우드 동기화 대상에 포함.

중요:
- 이번 버전은 Apps Script도 v16으로 교체 후 새 버전 배포가 필요합니다.
- 기존 Meta 시트 구조는 그대로 사용하므로 setupPortfolioSync()를 다시 실행할 필요는 없습니다.
